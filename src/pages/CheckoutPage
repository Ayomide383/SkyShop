import React, { useState, useMemo } from 'react';
import {
  Search,
  Heart,
  ShoppingBag,
  User,
  ChevronRight,
  ChevronDown,
  User as UserIcon,
  Mail,
  Phone,
  MapPin,
  Building,
  CreditCard,
  Calendar,
  Lock,
  ShieldCheck,
  Truck,
  Headphones,
  Watch,
  Backpack,
  Footprints,
  Landmark,
  Banknote,
  Sparkles,
  Check,
  AlertCircle
} from 'lucide-react';

const INITIAL_PRODUCTS = [
  {
    id: 1,
    name: 'Wireless Headphones',
    qty: 1,
    price: 59.99,
    bgColor: 'bg-blue-100',
    icon: Headphones,
    iconColor: 'text-blue-600',
  },
  {
    id: 2,
    name: 'Smart Watch',
    qty: 1,
    price: 89.99,
    bgColor: 'bg-slate-100',
    icon: Watch,
    iconColor: 'text-slate-800',
  },
  {
    id: 3,
    name: 'Urban Backpack',
    qty: 1,
    price: 39.99,
    bgColor: 'bg-sky-100',
    icon: Backpack,
    iconColor: 'text-sky-700',
  },
  {
    id: 4,
    name: 'Sports Sneakers',
    qty: 1,
    price: 69.99,
    bgColor: 'bg-indigo-100',
    icon: Footprints,
    iconColor: 'text-indigo-600',
  },
];

function Header({ totalItemCount }) {
  return (
    <header className="bg-white border-b border-slate-100 sticky top-0 z-40">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex items-center justify-between gap-4">
        {/* Logo */}
        <div className="flex items-center gap-12">
          <a href="#" className="text-2xl font-black text-[#0B2545] tracking-tight flex items-center gap-0.5">
            <span>New</span>
            <span className="text-blue-600">Logo</span>
          </a>

          {/* Navigation Links */}
          <nav className="hidden lg:flex items-center space-x-8 text-sm font-medium text-slate-600">
            <a href="#" className="hover:text-blue-600 transition-colors">Home</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Shop</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Categories</a>
            <a href="#" className="hover:text-blue-600 transition-colors">Deals</a>
            <a href="#" className="hover:text-blue-600 transition-colors">About Us</a>
          </nav>
        </div>

        {/* Search Bar */}
        <div className="flex-1 max-w-md mx-4 hidden md:block">
          <div className="relative">
            <input
              type="text"
              placeholder="Search for products..."
              className="w-full bg-slate-50 hover:bg-slate-100/80 focus:bg-white text-slate-700 text-sm pl-4 pr-10 py-2.5 rounded-full border border-slate-200 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition-all placeholder:text-slate-400"
            />
            <Search className="w-4 h-4 text-blue-600 absolute right-3.5 top-1/2 -translate-y-1/2 cursor-pointer" />
          </div>
        </div>

        {/* User Action Icons */}
        <div className="flex items-center gap-5">
          <button className="p-2 text-slate-600 hover:text-blue-600 hover:bg-slate-50 rounded-full transition-colors relative" aria-label="Wishlist">
            <Heart className="w-5 h-5" />
          </button>
          <button className="p-2 text-slate-600 hover:text-blue-600 hover:bg-slate-50 rounded-full transition-colors relative" aria-label="Cart">
            <ShoppingBag className="w-5 h-5" />
            {totalItemCount > 0 && (
              <span className="absolute top-1 right-1 bg-blue-600 text-white text-[10px] font-bold w-4 h-4 rounded-full flex items-center justify-center ring-2 ring-white">
                {totalItemCount}
              </span>
            )}
          </button>
          <button className="p-2 text-slate-600 hover:text-blue-600 hover:bg-slate-50 rounded-full transition-colors" aria-label="Profile">
            <User className="w-5 h-5" />
          </button>
        </div>
      </div>
    </header>
  );
}

function PageHeader() {
  return (
    <section className="bg-gradient-to-r from-[#EEF5FF] via-[#E6F0FA] to-[#E2EEFF] border-b border-blue-100/60 relative overflow-hidden">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-10 flex flex-col sm:flex-row sm:items-center justify-between relative z-10">
        <div className="space-y-2 max-w-xl">
          <div className="flex items-center gap-2 text-xs font-semibold text-slate-500">
            <a href="#" className="hover:text-blue-600 transition-colors">Home</a>
            <ChevronRight className="w-3 h-3 text-slate-400" />
            <span className="text-slate-800">Checkout</span>
          </div>
          <h1 className="text-3xl sm:text-4xl font-extrabold text-[#0F172A] tracking-tight">Checkout</h1>
          <p className="text-slate-600 text-sm sm:text-base">
            Complete your order and get your products delivered to your door.
          </p>
        </div>

        {/* Decorative 3D Illustration Graphic matching image */}
        <div className="hidden md:flex items-center justify-end relative h-32 w-72">
          <div className="relative flex items-center gap-2">
            {/* Bag Graphic */}
            <div className="w-24 h-28 bg-gradient-to-tr from-blue-600 to-sky-400 rounded-2xl shadow-xl shadow-blue-500/20 flex flex-col items-center justify-center text-white relative transform -rotate-6">
              <div className="w-8 h-8 border-2 border-white/60 rounded-t-full absolute -top-4"></div>
              <span className="text-2xl font-black tracking-tighter">N</span>
              <div className="absolute -top-2 -left-2 w-3 h-3 bg-sky-300 rounded-full animate-ping"></div>
            </div>
            {/* Box Graphic */}
            <div className="w-20 h-20 bg-gradient-to-br from-amber-600 to-amber-500 rounded-xl shadow-lg shadow-amber-900/10 flex items-center justify-center text-amber-100 relative transform rotate-12 -ml-6 top-3 border-t border-amber-300/40">
              <div className="w-full h-2 bg-amber-700/30 absolute top-3"></div>
              <div className="w-4 h-full bg-amber-700/20 absolute"></div>
            </div>
            {/* Shield Badge */}
            <div className="absolute -right-2 top-2 bg-blue-500 text-white p-2 rounded-xl shadow-md border-2 border-white transform rotate-6">
              <ShieldCheck className="w-5 h-5" />
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

function CheckoutForm({
  formData,
  formErrors,
  handleInputChange,
  paymentMethod,
  setPaymentMethod,
  isSubmitting,
  isCartEmpty
}) {
  return (
    <div className="lg:col-span-7 xl:col-span-8 space-y-6">
      
      {/* SECTION 1: Shipping Information */}
      <div className="bg-white rounded-2xl p-6 sm:p-8 shadow-sm border border-slate-100 space-y-6">
        <div className="flex items-center gap-3">
          <div className="w-8 h-8 rounded-full bg-blue-600 text-white font-bold text-sm flex items-center justify-center shadow-md shadow-blue-600/30">
            1
          </div>
          <div>
            <h2 className="text-lg font-bold text-slate-900">Shipping Information</h2>
            <p className="text-xs text-slate-500">Enter your delivery details</p>
          </div>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          {/* Full Name */}
          <div className="space-y-1.5 md:col-span-1">
            <label className="block text-xs font-semibold text-slate-700">
              Full Name <span className="text-red-500">*</span>
            </label>
            <div className="relative">
              <UserIcon className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
              <input
                type="text"
                name="fullName"
                value={formData.fullName}
                onChange={handleInputChange}
                placeholder="Your full name"
                className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                  formErrors.fullName ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
              />
            </div>
            {formErrors.fullName && (
              <p className="text-[11px] text-red-500 flex items-center gap-1">
                <AlertCircle className="w-3 h-3" /> Full name is required
              </p>
            )}
          </div>

          {/* Email Address */}
          <div className="space-y-1.5 md:col-span-1">
            <label className="block text-xs font-semibold text-slate-700">
              Email Address <span className="text-red-500">*</span>
            </label>
            <div className="relative">
              <Mail className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
              <input
                type="email"
                name="email"
                value={formData.email}
                onChange={handleInputChange}
                placeholder="your@email.com"
                className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                  formErrors.email ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
              />
            </div>
            {formErrors.email && (
              <p className="text-[11px] text-red-500 flex items-center gap-1">
                <AlertCircle className="w-3 h-3" /> Valid email address is required
              </p>
            )}
          </div>

          {/* Phone Number */}
          <div className="space-y-1.5 md:col-span-1">
            <label className="block text-xs font-semibold text-slate-700">
              Phone Number <span className="text-red-500">*</span>
            </label>
            <div className="relative">
              <Phone className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
              <input
                type="tel"
                name="phone"
                value={formData.phone}
                onChange={handleInputChange}
                placeholder="+234 801 234 5678"
                className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                  formErrors.phone ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
              />
            </div>
            {formErrors.phone && (
              <p className="text-[11px] text-red-500 flex items-center gap-1">
                <AlertCircle className="w-3 h-3" /> Phone number is required
              </p>
            )}
          </div>

          {/* Address */}
          <div className="space-y-1.5 md:col-span-1">
            <label className="block text-xs font-semibold text-slate-700">
              Address <span className="text-red-500">*</span>
            </label>
            <div className="relative">
              <MapPin className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
              <input
                type="text"
                name="address"
                value={formData.address}
                onChange={handleInputChange}
                placeholder="Street address, apartment, etc."
                className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                  formErrors.address ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
              />
            </div>
            {formErrors.address && (
              <p className="text-[11px] text-red-500 flex items-center gap-1">
                <AlertCircle className="w-3 h-3" /> Address is required
              </p>
            )}
          </div>

          {/* City / State / Zip Row */}
          <div className="grid grid-cols-1 sm:grid-cols-3 md:col-span-2 gap-4">
            {/* City */}
            <div className="space-y-1.5">
              <label className="block text-xs font-semibold text-slate-700">
                City <span className="text-red-500">*</span>
              </label>
              <div className="relative">
                <Building className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
                <input
                  type="text"
                  name="city"
                  value={formData.city}
                  onChange={handleInputChange}
                  placeholder="City"
                  className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                    formErrors.city ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                  } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
                />
              </div>
              {formErrors.city && (
                <p className="text-[11px] text-red-500 flex items-center gap-1">
                  <AlertCircle className="w-3 h-3" /> Required
                </p>
              )}
            </div>

            {/* State */}
            <div className="space-y-1.5">
              <label className="block text-xs font-semibold text-slate-700">
                State <span className="text-red-500">*</span>
              </label>
              <div className="relative">
                <MapPin className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2 pointer-events-none" />
                <select
                  name="state"
                  value={formData.state}
                  onChange={handleInputChange}
                  className={`w-full bg-white text-sm pl-10 pr-8 py-3 rounded-xl border ${
                    formErrors.state ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                  } ${formData.state ? 'text-slate-800' : 'text-slate-400'} appearance-none focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all cursor-pointer`}
                >
                  <option value="">Select state</option>
                  <option value="LA">Lagos</option>
                  <option value="AB">Abuja (FCT)</option>
                  <option value="RI">Rivers</option>
                  <option value="CA">California</option>
                  <option value="NY">New York</option>
                </select>
                <ChevronDown className="w-4 h-4 text-slate-400 absolute right-3.5 top-1/2 -translate-y-1/2 pointer-events-none" />
              </div>
              {formErrors.state && (
                <p className="text-[11px] text-red-500 flex items-center gap-1">
                  <AlertCircle className="w-3 h-3" /> Select state
                </p>
              )}
            </div>

            {/* Zip Code */}
            <div className="space-y-1.5">
              <label className="block text-xs font-semibold text-slate-700">
                Zip Code <span className="text-red-500">*</span>
              </label>
              <div className="relative">
                <Building className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
                <input
                  type="text"
                  name="zipCode"
                  value={formData.zipCode}
                  onChange={handleInputChange}
                  placeholder="Zip code"
                  className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                    formErrors.zipCode ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                  } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
                />
              </div>
              {formErrors.zipCode && (
                <p className="text-[11px] text-red-500 flex items-center gap-1">
                  <AlertCircle className="w-3 h-3" /> Required
                </p>
              )}
            </div>
          </div>
        </div>
      </div>

      {/* SECTION 2: Payment Method */}
      <div className="bg-white rounded-2xl p-6 sm:p-8 shadow-sm border border-slate-100 space-y-6">
        <div className="flex items-center gap-3">
          <div className="w-8 h-8 rounded-full bg-blue-600 text-white font-bold text-sm flex items-center justify-center shadow-md shadow-blue-600/30">
            2
          </div>
          <div>
            <h2 className="text-lg font-bold text-slate-900">Payment Method</h2>
            <p className="text-xs text-slate-500">Choose how you want to pay</p>
          </div>
        </div>

        {/* Payment Option Cards */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3.5">
          {/* Credit / Debit Card Option */}
          <div
            onClick={() => setPaymentMethod('card')}
            className={`p-4 rounded-xl border-2 cursor-pointer transition-all flex flex-col justify-between space-y-3 ${
              paymentMethod === 'card'
                ? 'border-blue-600 bg-blue-50/20 shadow-sm'
                : 'border-slate-200 hover:border-slate-300 bg-white'
            }`}
          >
            <div className="flex items-center justify-between">
              <div className={`w-4 h-4 rounded-full border flex items-center justify-center ${
                paymentMethod === 'card' ? 'border-blue-600 bg-blue-600' : 'border-slate-300'
              }`}>
                {paymentMethod === 'card' && <div className="w-1.5 h-1.5 bg-white rounded-full"></div>}
              </div>
              <CreditCard className={`w-5 h-5 ${paymentMethod === 'card' ? 'text-blue-600' : 'text-slate-500'}`} />
            </div>
            <div>
              <h3 className="text-xs font-bold text-slate-900">Credit / Debit Card</h3>
              <p className="text-[11px] text-slate-500 mt-0.5">Visa, Mastercard, Verve</p>
            </div>
          </div>

          {/* PayPal Option */}
          <div
            onClick={() => setPaymentMethod('paypal')}
            className={`p-4 rounded-xl border-2 cursor-pointer transition-all flex flex-col justify-between space-y-3 ${
              paymentMethod === 'paypal'
                ? 'border-blue-600 bg-blue-50/20 shadow-sm'
                : 'border-slate-200 hover:border-slate-300 bg-white'
            }`}
          >
            <div className="flex items-center justify-between">
              <div className={`w-4 h-4 rounded-full border flex items-center justify-center ${
                paymentMethod === 'paypal' ? 'border-blue-600 bg-blue-600' : 'border-slate-300'
              }`}>
                {paymentMethod === 'paypal' && <div className="w-1.5 h-1.5 bg-white rounded-full"></div>}
              </div>
              <span className="font-extrabold italic text-sm text-blue-700 tracking-tighter">PayPal</span>
            </div>
            <div>
              <h3 className="text-xs font-bold text-slate-900">PayPal</h3>
              <p className="text-[11px] text-slate-500 mt-0.5">Pay with PayPal</p>
            </div>
          </div>

          {/* Bank Transfer Option */}
          <div
            onClick={() => setPaymentMethod('bank')}
            className={`p-4 rounded-xl border-2 cursor-pointer transition-all flex flex-col justify-between space-y-3 ${
              paymentMethod === 'bank'
                ? 'border-blue-600 bg-blue-50/20 shadow-sm'
                : 'border-slate-200 hover:border-slate-300 bg-white'
            }`}
          >
            <div className="flex items-center justify-between">
              <div className={`w-4 h-4 rounded-full border flex items-center justify-center ${
                paymentMethod === 'bank' ? 'border-blue-600 bg-blue-600' : 'border-slate-300'
              }`}>
                {paymentMethod === 'bank' && <div className="w-1.5 h-1.5 bg-white rounded-full"></div>}
              </div>
              <Landmark className={`w-5 h-5 ${paymentMethod === 'bank' ? 'text-blue-600' : 'text-slate-500'}`} />
            </div>
            <div>
              <h3 className="text-xs font-bold text-slate-900">Bank Transfer</h3>
              <p className="text-[11px] text-slate-500 mt-0.5">Direct bank transfer</p>
            </div>
          </div>

          {/* Cash on Delivery Option */}
          <div
            onClick={() => setPaymentMethod('cod')}
            className={`p-4 rounded-xl border-2 cursor-pointer transition-all flex flex-col justify-between space-y-3 ${
              paymentMethod === 'cod'
                ? 'border-blue-600 bg-blue-50/20 shadow-sm'
                : 'border-slate-200 hover:border-slate-300 bg-white'
            }`}
          >
            <div className="flex items-center justify-between">
              <div className={`w-4 h-4 rounded-full border flex items-center justify-center ${
                paymentMethod === 'cod' ? 'border-blue-600 bg-blue-600' : 'border-slate-300'
              }`}>
                {paymentMethod === 'cod' && <div className="w-1.5 h-1.5 bg-white rounded-full"></div>}
              </div>
              <Banknote className={`w-5 h-5 ${paymentMethod === 'cod' ? 'text-blue-600' : 'text-slate-500'}`} />
            </div>
            <div>
              <h3 className="text-xs font-bold text-slate-900">Cash on Delivery</h3>
              <p className="text-[11px] text-slate-500 mt-0.5">Pay when you receive</p>
            </div>
          </div>
        </div>

        {/* Dynamic Card Form */}
        {paymentMethod === 'card' && (
          <div className="pt-2 space-y-4 animate-fadeIn">
            <h3 className="text-xs font-bold text-slate-800 uppercase tracking-wider">Card Details</h3>
            <div className="grid grid-cols-1 md:grid-cols-12 gap-4">
              {/* Card Number */}
              <div className="md:col-span-6 space-y-1.5">
                <label className="block text-xs font-semibold text-slate-700">
                  Card Number <span className="text-red-500">*</span>
                </label>
                <div className="relative">
                  <CreditCard className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
                  <input
                    type="text"
                    name="cardNumber"
                    value={formData.cardNumber}
                    onChange={handleInputChange}
                    placeholder="1234 5678 9012 3456"
                    maxLength={19}
                    className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                      formErrors.cardNumber ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                    } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
                  />
                </div>
                {formErrors.cardNumber && (
                  <p className="text-[11px] text-red-500 flex items-center gap-1">
                    <AlertCircle className="w-3 h-3" /> Card number required
                  </p>
                )}
              </div>

              {/* Expiry Date */}
              <div className="md:col-span-3 space-y-1.5">
                <label className="block text-xs font-semibold text-slate-700">
                  Expiry Date <span className="text-red-500">*</span>
                </label>
                <div className="relative">
                  <Calendar className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
                  <input
                    type="text"
                    name="expiryDate"
                    value={formData.expiryDate}
                    onChange={handleInputChange}
                    placeholder="MM / YY"
                    maxLength={5}
                    className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                      formErrors.expiryDate ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                    } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
                  />
                </div>
                {formErrors.expiryDate && (
                  <p className="text-[11px] text-red-500 flex items-center gap-1">
                    <AlertCircle className="w-3 h-3" /> Required
                  </p>
                )}
              </div>

              {/* CVV */}
              <div className="md:col-span-3 space-y-1.5">
                <label className="block text-xs font-semibold text-slate-700">
                  CVV <span className="text-red-500">*</span>
                </label>
                <div className="relative">
                  <Lock className="w-4 h-4 text-slate-400 absolute left-3.5 top-1/2 -translate-y-1/2" />
                  <input
                    type="password"
                    name="cvv"
                    value={formData.cvv}
                    onChange={handleInputChange}
                    placeholder="123"
                    maxLength={4}
                    className={`w-full bg-white text-sm pl-10 pr-3.5 py-3 rounded-xl border ${
                      formErrors.cvv ? 'border-red-500 bg-red-50/20' : 'border-slate-200'
                    } text-slate-800 placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500/20 focus:border-blue-600 transition-all`}
                  />
                </div>
                {formErrors.cvv && (
                  <p className="text-[11px] text-red-500 flex items-center gap-1">
                    <AlertCircle className="w-3 h-3" /> Required
                  </p>
                )}
              </div>
            </div>
          </div>
        )}

        {paymentMethod !== 'card' && (
          <div className="p-4 bg-slate-50 rounded-xl border border-slate-200 text-xs text-slate-600 flex items-center gap-2">
            <Sparkles className="w-4 h-4 text-blue-600 flex-shrink-0" />
            <span>
              You will be redirected to complete your payment securely with{' '}
              <strong className="capitalize text-slate-900">{paymentMethod}</strong> upon placing the order.
            </span>
          </div>
        )}

        {/* Pay Now Button & Security Info */}
        <div className="pt-2 space-y-3">
          <button
            type="submit"
            disabled={isSubmitting || isCartEmpty}
            className="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3.5 px-6 rounded-xl shadow-lg shadow-blue-600/25 active:scale-[0.99] transition-all flex items-center justify-center gap-2 text-sm sm:text-base disabled:opacity-50 disabled:cursor-not-allowed"
          >
            {isSubmitting ? (
              <div className="flex items-center gap-2">
                <div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></div>
                <span>Processing Order...</span>
              </div>
            ) : (
              <>
                <Lock className="w-4 h-4" />
                <span>Pay Now</span>
              </>
            )}
          </button>

          <p className="text-[12px] text-center text-slate-500 flex items-center justify-center gap-1.5">
            <ShieldCheck className="w-4 h-4 text-emerald-500" />
            Your payment information is secure and encrypted.
          </p>
        </div>
      </div>
    </div>
  );
}

function OrderSummary({ items, handleQtyChange, subtotal, tax, total, totalItemCount }) {
  const [isSummaryExpanded, setIsSummaryExpanded] = useState(true);

  return (
    <div className="lg:col-span-5 xl:col-span-4 space-y-6">
      <div className="bg-white rounded-2xl p-6 shadow-sm border border-slate-100 space-y-6 sticky top-24">
        
        {/* Header */}
        <div className="flex items-center justify-between pb-4 border-b border-slate-100">
          <h2 className="text-lg font-bold text-slate-900">Order Summary</h2>
          <button
            type="button"
            onClick={() => setIsSummaryExpanded(!isSummaryExpanded)}
            className="text-xs font-semibold text-blue-600 hover:text-blue-700 flex items-center gap-1 bg-blue-50 px-2.5 py-1 rounded-full transition-colors"
          >
            <span>{totalItemCount} {totalItemCount === 1 ? 'item' : 'items'}</span>
            <ChevronDown className={`w-3.5 h-3.5 transition-transform ${isSummaryExpanded ? 'rotate-180' : ''}`} />
          </button>
        </div>

        {/* Product List */}
        {isSummaryExpanded && (
          <div className="space-y-4 max-h-80 overflow-y-auto pr-1 divide-y divide-slate-100">
            {items.length === 0 ? (
              <p className="text-xs text-slate-400 text-center py-4">Your cart is empty.</p>
            ) : (
              items.map((item) => {
                const IconComponent = item.icon;
                return (
                  <div key={item.id} className="pt-3 first:pt-0 flex items-center justify-between gap-3">
                    <div className="flex items-center gap-3">
                      <div className={`w-12 h-12 rounded-xl ${item.bgColor} flex items-center justify-center flex-shrink-0 relative`}>
                        <IconComponent className={`w-6 h-6 ${item.iconColor}`} />
                      </div>
                      <div>
                        <h4 className="text-xs font-bold text-slate-900 leading-snug">{item.name}</h4>
                        <div className="flex items-center gap-2 mt-1">
                          <span className="text-[11px] text-slate-500">Qty: {item.qty}</span>
                          <div className="flex items-center gap-1 bg-slate-100 rounded px-1">
                            <button
                              type="button"
                              onClick={() => handleQtyChange(item.id, -1)}
                              className="text-xs text-slate-600 hover:text-black px-1 font-bold"
                              title="Decrease quantity"
                            >
                              -
                            </button>
                            <button
                              type="button"
                              onClick={() => handleQtyChange(item.id, 1)}
                              className="text-xs text-slate-600 hover:text-black px-1 font-bold"
                              title="Increase quantity"
                            >
                              +
                            </button>
                          </div>
                        </div>
                      </div>
                    </div>
                    <span className="text-xs font-bold text-slate-900">
                      ${(item.price * item.qty).toFixed(2)}
                    </span>
                  </div>
                );
              })
            )}
          </div>
        )}

        {/* Price Breakdown */}
        <div className="space-y-2.5 pt-2 border-t border-slate-100 text-xs">
          <div className="flex justify-between text-slate-600">
            <span>Subtotal ({totalItemCount} items)</span>
            <span className="font-semibold text-slate-800">${subtotal.toFixed(2)}</span>
          </div>
          <div className="flex justify-between text-slate-600">
            <span>Shipping</span>
            <span className="font-bold text-blue-600">Free</span>
          </div>
          <div className="flex justify-between text-slate-600">
            <span>Tax (7%)</span>
            <span className="font-semibold text-slate-800">${tax.toFixed(2)}</span>
          </div>
        </div>

        {/* Total Banner Box */}
        <div className="bg-[#EDF5FF] rounded-2xl p-4 flex items-center justify-between">
          <span className="text-sm font-bold text-slate-900">Total</span>
          <span className="text-2xl font-black text-blue-600">${total.toFixed(2)}</span>
        </div>

        {/* Protection Notice Box */}
        <div className="bg-blue-50/60 rounded-xl p-3.5 flex items-start gap-3 border border-blue-100/50">
          <ShieldCheck className="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" />
          <p className="text-[11px] text-slate-600 leading-relaxed">
            <strong className="text-slate-800 block">Your order is protected</strong>
            We use secure encryption to keep your payment information safe.
          </p>
        </div>

        {/* Trust Badges */}
        <div className="grid grid-cols-3 gap-2 pt-2 border-t border-slate-100 text-center">
          <div className="flex flex-col items-center gap-1 p-1">
            <Truck className="w-4 h-4 text-blue-600" />
            <span className="text-[10px] font-bold text-slate-800 leading-none">Free Shipping</span>
            <span className="text-[9px] text-slate-400">On orders over $50</span>
          </div>
          <div className="flex flex-col items-center gap-1 p-1 border-x border-slate-100">
            <ShieldCheck className="w-4 h-4 text-blue-600" />
            <span className="text-[10px] font-bold text-slate-800 leading-none">Secure Payment</span>
            <span className="text-[9px] text-slate-400">100% secure checkout</span>
          </div>
          <div className="flex flex-col items-center gap-1 p-1">
            <Headphones className="w-4 h-4 text-blue-600" />
            <span className="text-[10px] font-bold text-slate-800 leading-none">24/7 Support</span>
            <span className="text-[9px] text-slate-400">We're here to help</span>
          </div>
        </div>

      </div>
    </div>
  );
}

function OrderSuccessModal({ formData, paymentMethod, total, onClose }) {
  return (
    <div className="fixed inset-0 z-50 bg-slate-900/60 backdrop-blur-sm flex items-center justify-center p-4">
      <div className="bg-white rounded-3xl p-8 max-w-md w-full shadow-2xl text-center space-y-5 transform animate-in zoom-in-95 duration-200">
        <div className="w-16 h-16 bg-emerald-100 text-emerald-600 rounded-full flex items-center justify-center mx-auto ring-8 ring-emerald-50">
          <Check className="w-8 h-8 stroke-[3]" />
        </div>
        
        <div className="space-y-2">
          <h3 className="text-2xl font-black text-slate-900">Order Placed Successfully!</h3>
          <p className="text-xs text-slate-500">
            Thank you, <strong className="text-slate-800">{formData.fullName || 'Valued Customer'}</strong>. We have sent a confirmation email to <strong className="text-slate-800">{formData.email || 'your email'}</strong>.
          </p>
        </div>

        <div className="bg-slate-50 p-4 rounded-2xl text-left text-xs space-y-2 border border-slate-100">
          <div className="flex justify-between">
            <span className="text-slate-500">Order Number:</span>
            <span className="font-bold text-slate-800">#NL-89421</span>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Payment Method:</span>
            <span className="font-bold text-slate-800 uppercase">{paymentMethod}</span>
          </div>
          <div className="flex justify-between">
            <span className="text-slate-500">Total Paid:</span>
            <span className="font-bold text-blue-600">${total.toFixed(2)}</span>
          </div>
        </div>

        <button
          onClick={onClose}
          className="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-xl transition-all text-sm shadow-md"
        >
          Continue Shopping
        </button>
      </div>
    </div>
  );
}

export default function App() {
  const [items, setItems] = useState(INITIAL_PRODUCTS);
  const [paymentMethod, setPaymentMethod] = useState('card');
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [isOrderComplete, setIsOrderComplete] = useState(false);

  // Form State
  const [formData, setFormData] = useState({
    fullName: '',
    email: '',
    phone: '',
    address: '',
    city: '',
    state: '',
    zipCode: '',
    cardNumber: '',
    expiryDate: '',
    cvv: '',
  });

  const [formErrors, setFormErrors] = useState({});

  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setFormData((prev) => ({ ...prev, [name]: value }));
    if (formErrors[name]) {
      setFormErrors((prev) => ({ ...prev, [name]: false }));
    }
  };

  const handleQtyChange = (id, delta) => {
    setItems((prev) =>
      prev
        .map((item) => {
          if (item.id === id) {
            const newQty = item.qty + delta;
            return newQty > 0 ? { ...item, qty: newQty } : null;
          }
          return item;
        })
        .filter(Boolean)
    );
  };

  // Calculations
  const subtotal = useMemo(() => {
    return items.reduce((acc, item) => acc + item.price * item.qty, 0);
  }, [items]);

  const totalItemCount = useMemo(() => {
    return items.reduce((acc, item) => acc + item.qty, 0);
  }, [items]);

  const tax = useMemo(() => subtotal * 0.07, [subtotal]);
  const shipping = 0.0;
  const total = useMemo(() => subtotal + tax + shipping, [subtotal, tax]);

  const handlePayNow = (e) => {
    e.preventDefault();
    const errors = {};
    if (!formData.fullName.trim()) errors.fullName = true;
    if (!formData.email.trim()) errors.email = true;
    if (!formData.phone.trim()) errors.phone = true;
    if (!formData.address.trim()) errors.address = true;
    if (!formData.city.trim()) errors.city = true;
    if (!formData.state) errors.state = true;
    if (!formData.zipCode.trim()) errors.zipCode = true;

    if (paymentMethod === 'card') {
      if (!formData.cardNumber.trim()) errors.cardNumber = true;
      if (!formData.expiryDate.trim()) errors.expiryDate = true;
      if (!formData.cvv.trim()) errors.cvv = true;
    }

    if (Object.keys(errors).length > 0) {
      setFormErrors(errors);
      window.scrollTo({ top: 200, behavior: 'smooth' });
      return;
    }

    setIsSubmitting(true);
    setTimeout(() => {
      setIsSubmitting(false);
      setIsOrderComplete(true);
    }, 1200);
  };

  return (
    <div className="min-h-screen bg-[#F8FAFC] text-slate-800 font-sans antialiased pb-16">
      <Header totalItemCount={totalItemCount} />
      <PageHeader />

      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-8">
        <form onSubmit={handlePayNow} className="grid grid-cols-1 lg:grid-cols-12 gap-8">
          <CheckoutForm
            formData={formData}
            formErrors={formErrors}
            handleInputChange={handleInputChange}
            paymentMethod={paymentMethod}
            setPaymentMethod={setPaymentMethod}
            isSubmitting={isSubmitting}
            isCartEmpty={items.length === 0}
          />

          <OrderSummary
            items={items}
            handleQtyChange={handleQtyChange}
            subtotal={subtotal}
            tax={tax}
            total={total}
            totalItemCount={totalItemCount}
          />
        </form>
      </main>

      {isOrderComplete && (
        <OrderSuccessModal
          formData={formData}
          paymentMethod={paymentMethod}
          total={total}
          onClose={() => setIsOrderComplete(false)}
        />
      )}
    </div>
  );
}